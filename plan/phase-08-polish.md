# Фаза 8 — Производительность, доступность, полировка

**Цель:** соответствие блоку «Производительность и UX» из [`../TODO.md`](../TODO.md): режим перчатки, крупный шрифт, высокий контраст; энергосбережение GPS и карты; поддержка планшетов, поворота, split-view; расширение локализаций (DE, FR, ES, IT) после стабилизации RU/EN.

**Зависимости:** все основные функции предыдущих фаз в рабочем состоянии для целевого релиза.

**Ссылки:** [`../TODO.md`](../TODO.md), **[`../Подробный план реализации.md`](../Подробный%20план%20реализации.md)** (§9 качество, локализация, пороги покрытия).

---

## Связь с финальным планом

- **TDD / качество:** golden-тесты ключевых экранов где уместно; регрессия доступности (контраст, размер hit-target).
- **Логирование:** для release — согласованные уровни (`info` сценарии, `warning` деградации, `error` с `error_code`).
- **i18n:** новые локали только через ARB и `flutter gen-l10n`; проверка переполнения на планшетах и длинных строках.

---

## Шаги и артефакты

| Шаг | Действие | Выход |
|-----|----------|--------|
| 8.1 | Режим перчатки: увеличенные hit-targets; крупный шрифт; high contrast theme | Настройки доступности ([`settings_screen.dart`](../mobile/lib/features/settings/settings_screen.dart), [`accessibility_preferences_controller.dart`](../mobile/lib/core/accessibility_preferences_controller.dart)) |
| 8.2 | Профили энергии: частота GPS, FPS карты, отключение второстепенных слоёв в фоне | Preset eco/passage/precision — интервал трека, AIS-демо; eco снимает слои карты при **paused** ([`energy_profile_controller.dart`](../mobile/lib/core/energy_profile_controller.dart)) |
| 8.3 | Layout планшета; rotation; split-view (карта + журнал/погода) где уместно | `NavigationRail` при ширине ≥720px ([`shell_screen.dart`](../mobile/lib/features/shell/shell_screen.dart)); split-view карта+панель — отложено за рамками MVP |
| 8.4 | Переводы DE, FR, ES, IT; проверка длины строк в UI | ARB `app_de`/`app_fr`/`app_es`/`app_it` + выбор в [`language_sheet.dart`](../mobile/lib/features/settings/language_sheet.dart) |

---

## Детализация

### 8.2 Энергия

- Замеры battery profiling на типичном 4–8 часовом сеансе с включённым экраном карты.

### 8.4 Локали

- RTL не входит в MVP спеки `TODO.md`, но учесть архитектурно при росте.

---

## Критерии приёмки

- Чеклист доступности пройден (минимальный набор WCAG-подобных критериев для мобильных).
- Нет регрессии FPS карты при включении night mode и high contrast.
- Все пользовательские строки вынесены в ARB/аналог для новых языков.

---

## Полевое тестирование (из TODO)

- Солнечный свет, поляризационные очки, влажные руки, вибрация двигателя, холод — задокументировать результаты.

---

## Завершение проекта дорожной карты

После Фазы 8 — релизный кандидат для магазинов и сбор обратной связи для следующих эпиков (NMEA 2000, бронирования, расширенный social).

### Критерии релиза (MVP+)

- [ ] CI: покрытие не ниже порогов финального плана; `flutter test --coverage` зелёный.
- [ ] Цепочка аудита для событий безопасности и ключевых действий из Приложения A включена и проверена на тестовых сценариях.
